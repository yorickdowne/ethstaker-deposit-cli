name: ci-runner
run-name: ${{ github.actor }} is testing and measuring code quality
concurrency:
  group: ci-runner-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  actions: read
on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, labeled, unlabeled]
    branches: [main]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: 3.14
          cache: 'pip'
      - name: Install uv
        # This pinned action came from astral-sh/setup-uv@v10.0.1, releases can be found on https://github.com/astral-sh/setup-uv/releases
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
      - name: Install pre-commit
        run: uv sync --only-group dev --no-install-project
      - name: Cache pre-commit environments
        uses: actions/cache@v6.1.0
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}
      - name: Run pre-commit
        run: uv run pre-commit run --all-files --show-diff-on-failure
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --require-hashes -r requirements.txt -r requirements_test.txt
      # Redundant with pre-commit, kept so we don't depend on what pre-commit does
      - name: Run type checker
        run: python -m mypy --config-file mypy.ini -p ethstaker_deposit
      # Redundant with pre-commit, kept so we don't depend on what pre-commit does
      - name: Run linter
        run: ruff check ./ethstaker_deposit ./tests
      - name: Check dependency sync
        run: python scripts/check_dependencies_sync.py
      - name: Audit Python dependencies for known vulnerabilities
        run: pip-audit -r requirements.txt -r requirements_test.txt -r build_configs/windows/requirements.txt --no-deps --strict --desc
      - name: Setup Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 'latest'
      - name: Install jsonlint
        run: npm install -g @prantlf/jsonlint@16.0.0
      - name: Validate JSON files
        run: |
          find ./ethstaker_deposit/intl -name "*.json" -exec jsonlint --no-duplicate-keys --quiet --compact {} \;
  ci-runner:
    needs: lint
    if: |
      contains(github.event.pull_request.labels.*.name, 'run-tests') ||
      github.event_name == 'push'
    permissions:
      contents: read
      actions: write
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-26-intel, macos-latest, windows-latest, windows-11-arm]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        exclude:
          - os: windows-11-arm
            python-version: "3.10"
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --require-hashes -r requirements.txt -r requirements_test.txt
      - name: Run tests
        env:
          PYTHONWARNINGS: error::DeprecationWarning,error::PendingDeprecationWarning
        run: |
          coverage run --data-file=.coverage.${{ matrix.os }}.${{ matrix.python-version }} -m pytest tests
      - name: Upload coverage data
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.os }}-${{ matrix.python-version }}
          path: .coverage.${{ matrix.os }}.${{ matrix.python-version }}
          retention-days: 1
          if-no-files-found: error
          include-hidden-files: true
  coverage-combine:
    needs: ci-runner
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      actions: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: 3.14
          cache: 'pip'
      - name: Download coverage data
        uses: actions/download-artifact@v8
      - name: Merge coverage data
        id: merge
        run: |
          python -m pip install --upgrade pip
          pip install --require-hashes -r requirements.txt -r requirements_test.txt
          coverage combine coverage*
          coverage html
      - name: Upload final coverage html report
        uses: actions/upload-artifact@v7
        id: upload-html
        with:
          name: coverage-html-report
          path: htmlcov
      - name: Generate text report
        run: |
          mkdir results
          {
            echo "Test Coverage: [Download HTML Report](${{ steps.upload-html.outputs.artifact-url }})" ;
            echo ;
            echo \`\`\` ;
            coverage report ;
            echo \`\`\` ;
          } >> results/coverage-text-report
      - name: Upload final coverage text report
        uses: actions/upload-artifact@v7
        with:
          name: results
          path: results/
